package:
  name: "jpype"
  version: "dev"

source:
  path: ../../

build:
  number: 0
  script: python setup.py install

requirements:
  build:
    - numpy x.x
    - python
    - setuptools

  run:
    - numpy x.x
    - python

test:
  requires:
    - mock
    - pytest
    - pytest-faulthandler
  files:
    - compile_java_classes.py
  source_files:
    - test/*
    - setup.cfg
