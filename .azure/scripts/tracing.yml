# This job verifies that nothing is broken in the tracing compile
steps:
- task: UsePythonVersion@0
  inputs:
    versionSpec: '3.8'
- script: |
    pip install -v -e . -C cmake.args="-DENABLE_TRACING=ON"
  displayName: 'Build with tracing'

