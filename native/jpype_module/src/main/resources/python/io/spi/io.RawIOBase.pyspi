kind: class
module: io
class: RawIOBase
interface: python.io.PyRawIOBase
---
METHODS = {
    ".read": lambda x, size=-1: x.read(size),
    ".readall": lambda x: x.readall(),
    ".readinto": lambda x, b: x.readinto(b),
    ".write": lambda x, b: x.write(b),
}
